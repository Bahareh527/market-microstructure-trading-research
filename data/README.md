# Data

sample_lob.csv is a deterministic synthetic sample generated with seed 42. It is
included only so visitors can inspect the schema immediately. Regenerate it with:

    python scripts/generate_sample_data.py

No real orders, accounts, instruments, or exchange feeds are included.
